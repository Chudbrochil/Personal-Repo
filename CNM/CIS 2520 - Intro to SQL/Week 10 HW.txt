1.
CREATE TABLE GRAD_CANDIDATES 
(	
  STUDENT_ID NUMBER(6,0),
	LAST_NAME VARCHAR2(25) CONSTRAINT GRAD_LAST_NAME_NN NOT NULL, 
	FIRST_NAME VARCHAR2(25) CONSTRAINT GRAD_FIRST_NAME_NN NOT NULL, 
	CREDITS NUMBER(3,0), 
	GRADUATION_DATE DATE, 
    CONSTRAINT GRAD_ID_PK PRIMARY KEY(STUDENT_ID) 
);


2.
ALTER TABLE GRAD_CANDIDATES
ADD MAJOR_ID NUMBER(3);


Next section

1.
CREATE TABLE GLOBAL_LOCATIONS
(	
  ID NUMBER(4,0),
  LOC_NAME VARCHAR2(20), 
  DATE_OPENED DATE CONSTRAINT GLOB_DATE_NN NOT NULL,
  ADDRESS VARCHAR2(30) CONSTRAINT GLOB_ADDRESS NOT NULL,
  CITY VARCHAR2(20) CONSTRAINT GLOB_CITY NOT NULL,
  ZIP_POSTAL VARCHAR2(20),
  PHONE VARCHAR2(15),
  EMAIL VARCHAR2(80),
  MANAGER_ID NUMBER(4,0),
  CONTACT VARCHAR2(40),
    CONSTRAINT GLOB_ID_PK PRIMARY KEY(ID),
    CONSTRAINT GLOB_EMAIL_UK UNIQUE(EMAIL)
);

2.
CREATE TABLE ANIMALS
(	
  ANIMAL_ID NUMBER(6),
	NAME VARCHAR2(25) CONSTRAINT ANIMALS_NAME_NN NOT NULL, 
	LICENSE_TAG_NO NUMBER(10) CONSTRAINT ANIMALS_TAG_NN NOT NULL,
  ADMIT_DATE DATE CONSTRAINT ANIMALS_ADMIT_NN NOT NULL,
  ADOPTION_ID NUMBER(5) CONSTRAINT ANIMALS_ADOPT_NN NOT NULL,
  VACCINATION_ID NUMBER(4) CONSTRAINT ANIMALS_VACC_NN NOT NULL,
    CONSTRAINT ANIMALS_ANIMAL_ID_PK PRIMARY KEY(ANIMAL_ID),
    CONSTRAINT ANIMALS_TAG_UK UNIQUE(LICENSE_TAG_NO)
);

4.
INSERT INTO ANIMALS(ANIMAL_ID, NAME, LICENSE_TAG_NO, ADMIT_DATE, ADOPTION_ID, VACCINATION_ID)
VALUES(101, 'Spot', 35540, '10-OCT-04', 205, 4000);

SELECT *
FROM ANIMALS;

5.
CREATE TABLE VACCINATIONS
(	
  VACCINATION_ID NUMBER(4),
	VACCINATION_NAME VARCHAR2(20) CONSTRAINT VACCINATION_ID_NAME_NN NOT NULL, 
	VACCINATION_EXP_DATE DATE CONSTRAINT VACCINATION_ID_EXP_NN NOT NULL,
    CONSTRAINT VACCINATION_ID_PK PRIMARY KEY(VACCINATION_ID)
);

INSERT INTO VACCINATIONS(VACCINATION_ID, VACCINATION_NAME, VACCINATION_EXP_DATE)
VALUES(4000, 'parvo', '10-NOV-10');

6.
ALTER TABLE ANIMALS
ADD FOREIGN KEY (VACCINATION_ID) 
REFERENCES VACCINATIONS(VACCINATION_ID);

7. A foreign key value has no matching primary key value. It needs a matching 4001 Vaccination_ID. In order to add this child record I will need to add the 4001 vaccination_ID lower in the word doc.

INSERT INTO VACCINATIONS(VACCINATION_ID, VACCINATION_NAME, VACCINATION_EXP_DATE)
VALUES(4001, 'Wild Dog Syndrome', '20-JAN-12');

INSERT INTO ANIMALS(ANIMAL_ID, NAME, LICENSE_TAG_NO, ADMIT_DATE, ADOPTION_ID, VACCINATION_ID)
VALUES(102, 'Princess', 35545, '15-NOV-10', 209, 4001);

SELECT *
FROM VACCINATIONS;

SELECT *
FROM ANIMALS;





